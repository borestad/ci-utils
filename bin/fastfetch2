#!/usr/bin/env bash

# Extra args
type -p pveversion &> /dev/null && args="-l proxmox"

timeout 2s fastfetch \
  -s Title:Separator:OS:Host:Kernel:Uptime:Packages:CPU:GPU:Memory:Disk:Battery:PowerAdapter:Separator:LocalIP:command:PublicIp \
  --pipe false \
  ${args} | \tac | \tac
